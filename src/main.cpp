extern "C"{
    #include "..\include\duration.hpp"
    #include <stdio.h>
}

// File for sandboxing and trying out code
int main(int argc, char **argv)
{
    int alarm;
    if (alarm == NULL) 
        printf("Sup");
    return 0;
}